import * as React from 'react'
import { InstagramLogoIcon } from '@radix-ui/react-icons'
const InstagramIcon = () => {
  // fit the size to the size of the parent
  return <InstagramLogoIcon className="h-full w-full" />
}
export default InstagramIcon
